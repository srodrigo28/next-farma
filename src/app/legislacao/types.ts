export interface LegalReferenceItem {
  id: string;
  category: string;
  title: string;
  summary: string;
  source: string;
  updatedAt: string;
}

export interface LegalCategoryOption {
  id: string;
  label: string;
}
